version: '3.8'

services:
  dnd-character-manager:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: dnd_character_manager_web
    ports:
      - "8080:443"
    volumes:
      - ./ssl:/etc/nginx/ssl:ro
    restart: unless-stopped
